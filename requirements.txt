# ============================================================================
# Prompt2ML — host requirements (the orchestrator + agents).
# The execution sandbox has its own pinned set in docker/requirements.txt.
# ============================================================================

# --- Agent framework & LLM clients ---
google-adk>=1.0.0
google-genai>=1.0.0
litellm>=1.40.0
mcp>=1.0.0

# --- Dataset sources ---
kagglehub>=0.3.0
datasets>=2.19.0
huggingface_hub>=0.23.0

# --- Web research ---
# The PyPI distribution is `tavily-python`; it imports as `tavily`.
# Installing `tavily` pulls an unrelated project and leaves the import broken.
tavily-python>=0.5.0

# --- Sandbox control ---
# docker 7.1.0 + urllib3<2 + requests<2.32 together avoid the Windows
# named-pipe `heb_timeout` failure. Keep them pinned as a set; bumping one
# without the others reintroduces it.
docker==7.1.0
urllib3<2
requests<2.32

# --- Host-side data handling (profiling, validation, report stats) ---
pandas>=2.2.0,<3.0
numpy>=1.26.0,<3.0
scikit-learn>=1.4.0
pyarrow>=15.0.0
matplotlib>=3.8.0
seaborn>=0.13.0

# --- Config ---
python-dotenv>=1.0.0

# --- Test suite (M0 regression net) ---
pytest>=8.0.0
